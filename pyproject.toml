[project]
name = "mypyskindose"
version = "25.1.1"
description = "Tools and script for calculating peak skin dose and create dose maps for fluoroscopic exams from RDSR data (fork of PySkinDose)"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name="Max Hellström", email="max.hellstrom@gmail.com" },
    { name="Josef Lundman", email="josef@lundman.eu" },
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "pandas >= 2.2.3",
    "numpy >= 2.2.4",
    "pydicom >= 2.0",
    "numpy-stl >= 3.2.0",
    "plotly >= 4.13.3",
    "scipy >= 1.15.2",
    "tqdm",
    "psutil",
    "pillow >= 10.0.1",
    "kaleido",
    "rich",
]

[project.optional-dependencies]
gui = ["nicegui>=2.0.0", "pytest-asyncio>=0.24"]
gui-native = ["nicegui>=2.0.0", "pywebview", "pytest-asyncio>=0.24"]
dev = [
    "basedpyright>=1.23",
    "pandas-stubs>=2.2",
    "types-Pillow",
    "pip-audit>=2.7",
    "pre-commit>=4.0",
]

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true
zip-safe = false

[tool.setuptools.packages.find]
where = ["src"]
include = ["mypyskindose*"]
exclude = ["mypyskindose.egg-info*", "pyskindose.egg-info*"]


[project-urls]
"Homepage" = "https://github.com/kgrizz-git/MyPySkinDose"
"Bug Tracker" = "https://github.com/kgrizz-git/MyPySkinDose/issues"
"Documentation" = "https://github.com/kgrizz-git/MyPySkinDose/tree/main/docs"

[build-system]
requires = ["setuptools>=68.2.2"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = ["tests"]
pythonpath = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

[tool.isort]
profile = "black"
multi_line_output = 3
wrap_length = 79
known_first_party = ["mypyskindose"]
sections = ["FUTURE", "STDLIB", "THIRDPARTY", "FIRSTPARTY", "LOCALFOLDER"]
src_paths = ["src", "tests"]
skip_glob = ["**/__init__.py", "**/migrations", ".venv", ".venv39", ".github", ".pytest_cache"]

[tool.ruff]
line-length = 120

[tool.basedpyright]
pythonVersion = "3.10"
include = ["src/mypyskindose", "tests/unittests", "tests/gui", "tests/integrationtests", "scripts"]
exclude = [
    "**/__pycache__",
    "src/mypyskindose/table_data",
    "tests/manual_tests",
]
typeCheckingMode = "basic"
reportMissingTypeStubs = false
# Incremental typing: baseline file (.basedpyright/baseline.json) suppresses known issues; CI fails on new ones.

[tool.black]
line-length = 120
include = '\.pyi?$'
exclude = '''

(
  /(
      \.eggs         # exclude a few common directories in the
    | \.git          # root of the project
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.vscode
    | \.idea
    | \.github
    | \.venv
    | \.venv39
    | venv
    | _build
    | buck-out
    | build
    | dist
    | tools
    | pytest_cache
  )/
)
'''
