"""Geometry preview helpers (active exam index, sliced rdsr_df, event counts).

Part II of MULTI_EXAM_GEOMETRY_OFFSETS_PLAN adds ``clamp_active_exam_index``,
``rdsr_df_for_geometry_preview``, and related helpers here. Part I creates this
module as a placeholder so ``helpers.py`` can stay under the CI line cap.
"""
